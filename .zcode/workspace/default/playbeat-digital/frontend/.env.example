# PlayBeat Digital frontend
# API base URL (no trailing slash). Local dev uses the Vite proxy for /api.
VITE_API_URL=http://localhost:5000/api
# Stripe publishable key (optional, shown only if enabled in admin settings)
VITE_STRIPE_PUBLISHABLE_KEY=
